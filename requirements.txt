# Core (all platforms)
playwright
requests
certifi
python-dotenv
pystray
pillow

# No-API AdsPower GUI automation — Windows only.
# These drive the AdsPower desktop app when the Local API is permission-gated
# (9110). On macOS/Linux they are skipped (no-API GUI mode is Windows-only); the
# suite still runs there via the AdsPower Local API + Playwright.
pywinauto ; sys_platform == "win32"
pywin32 ; sys_platform == "win32"
pyautogui ; sys_platform == "win32"
opencv-python ; sys_platform == "win32"
numpy ; sys_platform == "win32"
